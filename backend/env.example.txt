# Configuration de sécurité
# Copiez ce fichier vers .env et modifiez les valeurs selon vos besoins

# CORS - Domaines autorisés (séparés par des virgules)
ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000,http://127.0.0.1:8000

# Rate Limiting
RATE_LIMIT_PER_MINUTE=10
RATE_LIMIT_PER_HOUR=100
RATE_LIMIT_UPLOAD_PER_HOUR=20

# Authentification API (optionnelle)
# Si défini, les endpoints sensibles nécessiteront un header X-API-Key
# API_KEY=votre-cle-secrete-ici

# Logging sécurisé (masque les données sensibles dans les logs)
SECURE_LOGGING=true

# Configuration des dossiers
UPLOAD_FOLDER=uploads
OUTPUT_FOLDER=outputs

# Timeout pour la suppression automatique des fichiers (en secondes)
FILE_TIMEOUT=3600
CLEANUP_INTERVAL=120

# Redis pour rate limiting en production (optionnel)
# REDIS_URL=redis://localhost:6379
